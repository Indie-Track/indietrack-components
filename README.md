# IndieTrack Components

Reusable Vue 3 components styled with Daisy UI.  
This package is shared across IndieTrack projects via PNPM workspaces or Git submodules.

## 🚀 Quick Start

```bash
pnpm install
pnpm run local
